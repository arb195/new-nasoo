import Button from '@mui/material/Button';
import React from 'react';

const PaginationBtn = ({ name }) => {
  return <Button variant="contained">{name}بعدی</Button>;
};

export default PaginationBtn;
