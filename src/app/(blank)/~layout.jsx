import AppLayout from '@/components/layout/appLayout/appLayout';

const Template = ({ children }) => { 
          <AppLayout type="blank">
            {children}
          </AppLayout>
};

export default Template;